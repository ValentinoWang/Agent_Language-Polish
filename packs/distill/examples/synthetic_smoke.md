# Synthetic smoke example

Two short texts from one synthetic author produce a `draft` StyleCard with evidence references. Human approval remains required; this fixture does not establish author similarity.
