# Synthetic smoke example

Input: `值得注意的是，项目在 2026 年处理了 12 个样本。`

Expected invariant: `2026` and `12` remain unchanged while removable framing may be deleted. This fixture checks deterministic wiring only.
